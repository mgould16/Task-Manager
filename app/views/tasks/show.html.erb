<%= @task.name %>
<%= @task.details %>
<h1><%= link_to "edit", edit_task_path(@task)%></h1>

<%= link_to "Delete", task_path(@task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
